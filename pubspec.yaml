name: dashboard
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  fl_chart: 0.36.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
